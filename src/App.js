import React from 'react';
import { Container } from '@mui/material';
import Weather from './components/Weather';

const App = () => {
    return (
        <Container>
            <Weather />
        </Container>
    );
};

export default App;
